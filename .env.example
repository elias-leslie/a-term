# Copy this file to .env.local for native runs or export the values in your shell.

# Required
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/terminal

# Optional database pool tuning
DB_POOL_MIN_SIZE=2
DB_POOL_MAX_SIZE=10
DB_POOL_TIMEOUT_SECONDS=10
DB_POOL_MAX_WAITING=20
DB_POOL_MAX_LIFETIME_SECONDS=1800
DB_POOL_MAX_IDLE_SECONDS=300
DB_POOL_RECONNECT_TIMEOUT_SECONDS=30

# Service
TERMINAL_PORT=8002
LOG_LEVEL=INFO
LOG_DIR=logs
NEXT_PUBLIC_AGENT_HUB_URL=
AGENT_HUB_URL=
SUMMITFLOW_API_BASE=

# CORS origins must be valid JSON
CORS_ORIGINS=["http://localhost:3002"]

# Uploads
MAX_FILE_SIZE_MB=10
UPLOAD_DIR=/home/YOUR_USER/terminal-uploads
UPLOAD_MAX_AGE_SECONDS=86400

# Maintenance
MAINTENANCE_ENABLED=true
MAINTENANCE_INTERVAL_SECONDS=900
MAINTENANCE_SESSION_PURGE_DAYS=7
